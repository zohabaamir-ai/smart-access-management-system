# Frontend environment — copy to `.env.local` and adjust if needed.
# `.env.local` is git-ignored; this template is committed for onboarding.

# Backend origin the SPA calls. Defaults to http://127.0.0.1:8000 when unset.
VITE_API_BASE_URL=http://127.0.0.1:8000
